<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="ru.ancientempires">

	<application
		android:allowBackup="true"
		android:icon="@mipmap/ic_launcher"
		android:label="@string/app_name"
		android:supportsRtl="true"
		android:theme="@style/AppTheme">
		<activity android:name=".activity.MainActivity">
			<intent-filter>
				<action android:name="android.intent.action.MAIN" />

				<category android:name="android.intent.category.LAUNCHER" />
			</intent-filter>
		</activity>
		<activity
			android:name=".activity.PlayMenuActivity"
			android:label="@string/title_activity_play_menu" />
		<activity android:name=".activity.LevelMenuActivity" /> <!-- label = название игры -->
		<activity android:name=".activity.PlayersConfigureActivity" /> <!-- label = название игры -->
		<activity android:name=".activity.GameActivity" /> <!-- label = название игры -->
		<activity
			android:name=".activity.SettingsActivity"
			android:label="@string/title_activity_settings">
		</activity>
		<activity
			android:name=".activity.EditorBaseActivity"
			android:label="@string/title_activity_editor_base">
		</activity>
		<activity
			android:name=".activity.EditorConfigureActivity"
			android:label="@string/title_activity_editor_choose">
		</activity>
		<activity
			android:name=".activity.EditorActivity"
			android:label="@string/title_activity_editor">
		</activity>
		<activity android:name=".activity.TestActivity" />
	</application>

</manifest>